# CASIA_REAL-FAKE_Image_Detector
This repository is the Final Project from the Advanced Machine Learning class. The project is about image detection, whether it's fake or real by using binary classification, CNN model. 


Note: Since my model.keras file size is too big, and I cannot upload the model file. I only uploaded .ipynb model file, by running all the code, it will generate/ create the model.keras file! 

Dataset: https://www.kaggle.com/code/shaft49/real-vs-fake-images-casia-dataset 

Note: In the Microsoft Azure Cloud deployment stage, the model was having error with .h5 file first. So, I changed using .keras file! Secondly, the deployment was successful but predicitons cannot be made and showing error. So, I have to generate another .keras file with new format, since the old .keras file was using the old format, the file size is also larger 200 MB. Right now, .keras file is only around 60 MB. 
